import { StyleSheet } from 'react-native';
// import { Colors } from 'styles';

export default StyleSheet.create({
  progress: {
    display: 'flex',
    flexDirection: 'row',
  },
  step: {
    flex: 1,
  },
});
