import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    height: 120,
    top: 0,
    left: 0,
    right: 0,
  },

  subContainer: {
    flex: 1,
  },
});
